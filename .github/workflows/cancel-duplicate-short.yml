name: Cancel duplicate Short run

on:
  push:
    paths:
      - ".github/workflows/cancel-duplicate-short.yml"

concurrency:
  group: youtube-ai-publisher-31960798967
  cancel-in-progress: true

jobs:
  stop-duplicate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cancelling duplicate Short run through repository concurrency."
